#include <stdio.h>

/**
	*
	* main - prints all single digit base 10 number
	*
	* Return: 0 on succes
	*
	*/

int main(void)
{
	int n;
	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
