#include "main.h"
#include <stdlib.h>

/**
	* _abs - computes the absolute value of an integer
	*
	* @i: absolute of number
	*
	* Return: Always 1 on negative and 0 on positive
	*
	*/

int _abs(int i)
{
	return (abs(i));

}
